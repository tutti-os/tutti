---
"@tutti-os/agent-gui": patch
---

Add an optional external Side presentation bridge and reusable Side surface so standalone Agent hosts can place the transient Side conversation in the shared right tool sidebar while existing embedded hosts retain the inline pane.
