---
"@tutti-os/agent-gui": minor
---

Add natural dismiss interactions for the Agent composer `@` mention palette. The palette now closes when clicking outside its bounds or when the user types whitespace immediately after `@`, matching the behavior of the `/` slash-command palette.
