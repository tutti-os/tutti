---
"@tutti-os/agent-activity-core": patch
"@tutti-os/agent-gui": patch
"@tutti-os/client-tuttid-ts": patch
---

Preserve typed agent capabilities while loading composer options before a session exists, so new conversations expose the same supported slash commands as existing sessions.
