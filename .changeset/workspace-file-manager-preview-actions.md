---
"@tutti-os/workspace-file-manager": minor
---

Add optional `previewActions` so hosts can declare an action row at the bottom of the preview panel. Copy and open opt in with a boolean and reuse the package's own session commands; download and share are enabled by passing a host handler.
