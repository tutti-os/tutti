---
"@tutti-os/workspace-file-reference": minor
---

Separate reference-picker source tabs, sidebar groups, and directory children into explicit runtime lifecycles. Add stale-while-revalidate tab metadata, per-open asynchronous sidebar loading, in-flight-only read deduplication, and runtime invalidation while keeping selection preparation live on every confirmation.
