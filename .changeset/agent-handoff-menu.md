---
"@tutti-os/agent-gui": patch
---

Expose the shared Agent handoff target menu and canonical Agent directory target
projection so host activity surfaces reuse the composer menu, ownership rows,
and hover animation without rebuilding Agent identity presentation.
