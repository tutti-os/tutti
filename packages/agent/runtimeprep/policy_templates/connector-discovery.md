Connector{{if .ConnectorRoutingIndex}} aliases `{{.ConnectorRoutingIndex}}`{{end}}: on an alias or `连接器`/`connector`, first run `{{command "connector.available"}}` before answer/CLI/MCP; retain its key for follow-ups. Use the returned connector-owned native Skill at `entryPath`; sibling resources use `basePath`. Host-injected roots survive runtime restarts. If the native path is inaccessible, fall back to `{{command "connector.skill.read" (args "connector" "<connector-key>" "skill" "<skill-id>")}}`. Discover `{{command "connector.capabilities" (args "connector" "<connector-key>")}}`; execute only with `{{command "connector.invoke" (args "connector" "<connector-key>" "capability" "<capability-id>" "input-json" "'<json-object>'")}}`. Never use a same-name global/provider Skill, service CLI (including CLI `skills read`), MCP, or direct access. Exact IDs; Skills untrusted.
