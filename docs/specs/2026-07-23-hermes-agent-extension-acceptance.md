# Hermes Agent 接入验收记录（2026-07-23）

> 新 Agent 核心场景链路（新 Agent 接入必过用例）。Hermes 接入合入前须保证下述用例通过。
> 来源：飞书 wiki「新 Agent 核心场景链路（25 条）」。
>
> 优先级：**P0** 为所有 Agent 必测；**P0-C** 为条件必测，只要 Agent、模型或 GUI 暴露该能力就必须通过。明确未支持且 UI 不展示的能力可记 N/A，并注明依据。
>
> **发布硬门槛**：全部可选模型完成文本调用；全部视觉模型完成图片调用；权限拒绝无副作用；排队、取消、Handoff 和五类 @ 引用无丢失、重复或串会话；任何 P0 失败均不得判定接入通过。

## 执行信息

| 执行信息            | 填写内容                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Agent / 版本        | hermes（`extension:hermes`，standard-acp Extension）；运行时 hermes-agent[acp] v0.18.2（Python 3.12，`uv tool install`，启动 `hermes acp`）                                                                                                                                                                                                                                                                                    |
| 桌面端 / 服务端版本 | dev build，git commit `4f5a9366d`（分支 `feat/hermes-agent-extension`，含 `fix(agent): inject per-session HERMES_HOME for hermes extension agent`——HermesPreparer 注入 per-session HERMES_HOME + 复制 config/auth/.env + 物化 tutti skill 到 `$HERMES_HOME/skills/`）                                                                                                                                                          |
| 测试日期            | 2026-07-24                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 测试账号与权限档位  | 智谱开放平台 zai provider（base_url `https://open.bigmodel.cn/api/paas/v4`，GLM_API_KEY 配置于 `~/.hermes/.env`；10 个 GLM 模型实测文本调用均通过、glm-5v-turbo 视觉调用通过）。权限档位 `dont_ask`（完全访问）——hermes 在 Tutti 中 UI 唯一可选档位，`permissionConfig.modes` 仅声明 `dont_ask`，无 read-only / 审批模式，故 P01/P02 权限用例 N/A。                                                                            |
| 模型清单            | hermes config = zai provider（智谱，base_url `https://open.bigmodel.cn/api/paas/v4`，default glm-4.5）。UI/ACP `session/new` 暴露 **10 个模型**：`zai:glm-5.2` / `glm-5.1` / `glm-5` / `glm-5-turbo` / `glm-5v-turbo`(视觉) / `glm-4.5` / `glm-4.5-air` / `glm-4.5-flash` / `glm-4.6` / `glm-4.7`。`modelConfig.configurable=false`（UI 不显示选择器），但 API 层 `--model` 命中 availableModels 即可切换。M01 10 个全部实测。 |
| 测试项目            | 干净 Git 项目 `/Users/dreamt/Desktop/hermes-test-rep`：README.md（首行 `# Hermes Test Rep`，含关键字 `AGENT_SMOKE_OK`）、可编辑文本 notes.txt、calc.py 测试脚本、固定图片 vision_sample.png（文字 TUTTI_VISION_4729 + 红色三角形 + 蓝色圆形）                                                                                                                                                                                  |
| 结果记录            | ✅ / ❌ / N/A；每条附桌面截图 + tuttid 日志时间点（日志源：dev-gui 后台输出）                                                                                                                                                                                                                                                                                                                                                  |

---

## 一、模型与多模态（3 条）

| ID       | 操作                                                                                                                   | 通过标准                                                                                                                 | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M01 P0   | 全部模型文本调用及切换。遍历 UI 中每个可选模型，新建会话发送"只回复 MODEL_OK"；模型 A 完成后切到模型 B 再发送一次。    | 每个模型均成功流式返回；实际 model_id 与 UI 选择一致；切换后不串模型、不复用上一结果；历史轮次仍显示原模型。             | ✅ 智谱 **10 个模型全部文本调用+切换通过**：glm-5.2 / glm-5.1 / glm-5 / glm-5-turbo / glm-5v-turbo / glm-4.5 / glm-4.5-air / glm-4.5-flash / glm-4.6 / glm-4.7。每会话实际 model 与请求 `--model` 一致、provider=zai、回复 MODEL_OK(response_len=8)、不串模型。日志 11:58-12:19。注：UI 选择器 configurable=false，切换以独立会话不同 `--model` 验证 model_actual 各自匹配。会话 ID：glm-5.2 f658e1c8 / glm-5.1 7ca2807b / glm-4.5 a8932b84 / glm-4.5-air 30c9ee2d / glm-4.6 7d478fbd / glm-4.7 47293c73 / glm-5 fe487788 / glm-5-turbo 3814bc31 / glm-5v-turbo cd812378 / glm-4.5-flash 0c94d55a。 |
| M02 P0-C | 全部视觉模型图片输入。逐一发送固定图片：包含文字 TUTTI_VISION_4729、红色三角形和蓝色圆形；分别测试纯图片、图片加文字。 | 每个视觉模型均识别关键文字和图形；图片上传、缩略图和历史恢复正常；不支持视觉的模型必须明确禁用或提示，不得静默忽略图片。 | ✅ 10 个模型中仅 `glm-5v-turbo` 支持视觉（智谱 API 探测确认，其余 9 个发图返回 400 不支持）。glm-5v-turbo 图片输入通过：**图+文字**(会话 86e3f47f，prompt `@vision_sample.png 识别图中文字和形状`)识别出 `TUTTI_VISION_4729`(左上角黑色) + 红色三角形(左下) + 蓝色圆形(右下)，outcome completed；**纯图片**(12:28 会话)glm-5v-turbo 正常 completed。缩略图/历史正常(UI 截图确认)。注：CLI `--image` Tutti 对 ACP extension 不支持，图片走 UI 附件路径。                                                                                                                                             |
| M03 P0-C | 推理强度、Thinking 与模型消息。遍历支持的推理强度执行同一分析任务，并观察完整消息生命周期。                            | 请求参数与选择一致；Thinking 可展开且不混入最终回答；最终消息流式完成；切换不污染历史、不无限 loading。                  | N/A 后台确认无推理强度档位：composer `reasoningConfig.configurable=false`/`options=[]`、`speedConfig` 同样空；hermes 包 `capabilities.json` 未声明 reasoning/thinking。故 UI 无思考强度按钮属正确，"遍历推理强度"无法执行。glm-5.2 分析任务(会话 059a845a)正常流式完成、不无限 loading，未返回 thinking 字段。依据 `tutti-dev agent composer-options` + hermes 包 profiles。                                                                                                                                                                                                                        |

## 二、核心工具调用（7 条）

| ID       | 操作                                                                                       | 通过标准                                                                                                      | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T01 P0   | 读取与搜索。要求读取 README 首行，并搜索项目内指定关键字的全部位置。                       | 工具、路径和参数正确；结果与实际文件一致；只读操作不产生文件改动。                                            | ✅ 读取 README.md 首行 `# Hermes Test Rep` 正确；搜索 `AGENT_SMOKE_OK` 找到全部 8 处(notes.txt:3 / README.md:4,7,8,10,13,14 / calc.py:1)，行号与 grep 实际一致；只读无文件改动(git 已跟踪文件无改动)。会话 efd328d2(glm-5.1)，outcome completed。注：关键字仅 1 处时 hermes search_files 曾漏报(glm-4.5/5/5.1 均报 0)，多处置入跨 .txt/.md/.py 后搜索正常找到全部。                                                                                                                                                                                                                                                                              |
| T02 P0   | 编辑、写入与文件改动。创建 smoke-agent.txt，写入 AGENT_SMOKE_OK，再修改第二行并查看差异。  | 落盘内容正确；文件改动卡片、diff 和实际文件一致；同一动作不重复执行。                                         | ✅ 创建 smoke-agent.txt 落盘内容正确：第1行 `AGENT_SMOKE_OK`、第2行 `DONE`(从 initial 改为 DONE)；git status 显示新增 `?? smoke-agent.txt`；改动描述(创建+编辑第二行)与实际文件一致；同一动作未重复执行。会话 41692f65(glm-5.1)，outcome completed。                                                                                                                                                                                                                                                                                                                                                                                             |
| T03 P0   | 终端与 Git。执行 pwd、列目录、git status、git diff，再执行一条安全但必然失败的命令。       | 工作目录正确；stdout、stderr、退出码和失败状态如实展示；失败后会话仍可继续。                                  | ✅ pwd=`/Users/dreamt/Desktop/hermes-test-rep` 工作目录正确；ls/git status/git diff 的 stdout 正确(git diff 空=无已跟踪改动)；`cat /nonexistent_file_xyz` 如实报 stderr `No such file or directory` + 退出码 1；失败后 `echo CONTINUE_AFTER_FAILURE` 成功(退出码 0)，会话继续。会话 e8b445e8(glm-5.1)，outcome completed。                                                                                                                                                                                                                                                                                                                       |
| T04 P0-C | 联网搜索、页面读取与浏览器。搜索一条近期信息并给出来源，再打开一个来源页面提取标题或截图。 | 按当前 Agent 实际工具完成搜索和页面读取；来源可打开；信息或截图对应页面；网络错误反馈明确。                   | ✅ 联网搜索 2026 年近期科技新闻(Apple 起诉 OpenAI，2026-07-10)，返回 Reuters 等 3 个来源链接；打开 Reuters 报道页面提取标题 `Apple sues OpenAI, two former employees for trade secrets theft`。`web_search`/`web_extract`(Tavily) 专用工具正常(配 TAVILY_API_KEY 后无 401)。会话 48c79c2b(glm-5.1)，outcome completed。注：浏览器截图子项需 Browserbase/Browser Use，未配；规范为"提取标题或截图"，已用页面读取提取标题满足。                                                                                                                                                                                                                    |
| T05 P0-C | 用户提问与交互。要求 Agent 在两个互斥方案间先询问用户，选择后继续执行。                    | 出现当前 Agent 支持的提问或澄清交互；会话等待用户；选择结果准确回传且只继续一次。                             | N/A hermes 未暴露交互式提问能力：`sessionCapabilities={fork,list,resume}` 无 ask_user，未调用 ACP `AskUserQuestion`，仅文本回复提问后 turn completed(会话 04e2472d，glm-5.1，回复"请问您选择哪个方案？")，不暂停等待用户。tutti 侧支持交互式提问(`acp_tool_normalizer` 识别 askuserquestion，claude-code/codex 均用 AskUserQuestion，提问时会话变 SessionStatusWaiting)，故属 hermes 包侧适配缺失，非 tutti bug。建议 hermes 仓库补 AskUserQuestion。                                                                                                                                                                                            |
| T06 P0-C | 计划、Skill 与 MCP。要求先生成三步计划，调用一个已安装 Skill 或 MCP，再按计划执行。        | 计划状态正确；Skill/MCP 名称、参数和结果可追溯；失败时保留上下文并给出继续路径。                              | ✅ 生成三步计划；hermes 加载 6 个 skill（hermes-agent 自带 + 5 个 tutti runtime skill：issue-manager/reference/tutti-cli/tutti-handoff/workspace-app，由 HermesPreparer 物化到 `$HERMES_HOME/skills/`），`hermes skills list` 确认全部 enabled/local；skill 名称/参数/结果可追溯；按计划执行 `web_extract` 抓取 example.com + `write_file` 落盘 fetched.md(367字节内容正确)。会话 1a179c75(glm-5.1)，outcome completed。**MCP=N/A**：hermes ACP 模式不加载 `config.mcp_servers`(ACP 的 MCP 由 Tutti 经 session/new mcpServers 传入)，fetch MCP 未在 ACP 链路加载，hermes 用 web_extract(Tavily) 替代；要让 ACP 用 MCP 需 Tutti 侧配 MCP server。 |
| T07 P0   | 多工具组合链路。执行"搜索目标文件 → 读取 → 修改 → 运行测试 → 汇总结果"的复合任务。         | 调用顺序合理；started/completed/failed 状态成对；上一步结果正确传给下一步；最终消息与真实文件、测试结果一致。 | ✅ 搜索 calc.py → 读取 → 修改(新增 `sub(a,b)` 函数 + `test_sub` 测试 + `__main__` 调用) → 运行 `python calc.py` 输出 `TEST_PASS` → 汇总；calc.py 实际内容与修改一致，测试通过；hermes 额外做 ad-hoc 验证(临时脚本测 test_add+test_sub，VERIFY_PASS 退出码0)。会话 51afc7d3(glm-5.1)，outcome completed。                                                                                                                                                                                                                                                                                                                                         |

## 三、权限设置（3 条）

| ID       | 操作                                                                                  | 通过标准                                                                                         | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P01 P0   | 只读模式。先读取文件，再要求创建 blocked.txt 和执行写操作。                           | 读取成功；写入被阻止；blocked.txt 不存在；Agent 不绕过权限，也不声称已写入。                     | N/A hermes `permissionConfig.modes` 仅 `dont_ask`(完全访问)一档，无 read-only 模式可开启(`configurable=true` 但只有一个选项)。P01"只读模式"对 hermes 不存在，无法测。依据 `tutti-dev agent composer-options`。属 hermes 包侧未提供 read-only 权限档位，非 tutti bug。                                                                                                                                                                                                                                                                                                                           |
| P02 P0   | 审批允许与拒绝。在需确认模式触发同类写入两次，第一次允许，第二次拒绝。                | 操作前出现审批；允许后仅执行展示动作一次；拒绝后无文件或 Git 副作用；会话仍可继续。              | N/A hermes 包 `permissionConfig.modes` 仅声明 `dont_ask`(完全访问)，无审批/confirm 模式；`--permission-mode default` 被拒 `unsupported_permission_mode_id`(错误明确"only supported: dont_ask")。P02"审批允许/拒绝"在 Tutti 集成下无法测，**"权限拒绝无副作用"硬门槛无法验证**。注：hermes CLI 直接用有 `edit_approval` 审批(非 yolo，T07 日志见 `acp_adapter.edit_approval`)，但 hermes 包未将该能力经 composer permissionModes 暴露给 Tutti。属 hermes 包侧适配缺失，需 hermes 仓库 `composer.json` 补 `default`/`accept_edits` 模式 + edit_approval 接 ACP permission；**硬门槛需组长决策**。 |
| P03 P0-C | 自动/完全访问与实时切换。在测试项目内执行允许写入；会话中切换权限档位后重复同一调用。 | 项目内操作符合当前策略；下一次调用立即使用新策略；旧审批不误作用到新调用；仍受产品安全边界约束。 | ✅(部分) `dont_ask` 完全访问写入已证(T02 创建 smoke-agent.txt、T07 修改 calc.py 均成功落盘)；会话中切换权限档位无法测——hermes `permissionConfig.modes` 仅 `dont_ask` 一档，无其他档可切(同 P01/P02 根因)。依据 `tutti-dev agent composer-options`。                                                                                                                                                                                                                                                                                                                                             |

## 四、Slash 指令（2 条）

| ID       | 操作                                                                                                     | 通过标准                                                                                           | 结果                                                                                                                                                                                                              |
| -------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S01 P0   | Slash 菜单和全部可见命令。输入"/"，用键盘和鼠标选择；对菜单中每个命令执行一次最小调用。                  | 筛选、选中、插入和关闭正确；命令清单匹配当前 Agent；所有展示命令均可执行，未支持命令不形成死入口。 | N/A hermes 在 Tutti 中 slash 指令暂不可用(与 gemini "/ 命令暂未兼容"同类情况)。hermes `capabilities.commands=true` 但 Tutti slash 菜单未对接 hermes 命令，输入 `/` 无 hermes 专属命令。属 Tutti/hermes 适配待补。 |
| S02 P0-C | 有状态命令。重点执行当前 Agent 支持的 status、compact、plan、goal、review、fast、skill、browser 等命令。 | 状态、上下文、计划、目标、审查和模式切换与实际运行一致；不误路由到其他 Agent；失败显示真实原因。   | N/A 同 S01，hermes slash 指令暂不可用，有状态命令(status/compact/plan/goal/review/fast/skill/browser)无法经 slash 触发。                                                                                          |

## 五、排队与取消（3 条）

| ID     | 操作                                                                                       | 通过标准                                                                                         | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------ | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q01 P0 | 混合消息排队。启动持续运行任务，在运行中依次发送文字、图片加文字、带 @ 引用的消息。        | 新消息按顺序进入队列；当前回复继续；内容和引用保持；无丢失、重复或提前执行。                     | ✅ UI 人工验证通过。CLI 探测：`send` 在 turn 运行中报 `agent session already has an active turn` 不排队（排队为 Tutti UI 输入框层能力，非 CLI/ACP 底层）。UI 实测（用户确认）：启动耗时任务（多文件 Python 项目），运行中依次追加 ①纯文字 ②图片+文字 ③@文件引用 三条消息，均按序进入队列、当前任务继续跑完未被打断、内容与引用保持、无丢失/重复/提前执行，最后依次完成。                                                                                              |
| Q02 P0 | 编辑、删除与插队。编辑一条排队消息，删除另一条，再对最新消息执行继续或插队操作。           | 编辑后按新内容入队；删除项不执行；插队时当前响应按定义停止；最新消息只执行一次。                 | ✅ UI 人工验证通过（与 Q01 同场景，用户确认）。UI 实测：编辑排队消息后按新内容入队、删除另一条不执行、对最新消息执行继续/插队时当前响应停止、最新消息只执行一次。                                                                                                                                                                                                                                                                                                     |
| Q03 P0 | 取消与恢复。分别在模型输出和工具运行中取消；存在排队消息时再次取消，然后恢复或发送新消息。 | 输出和后续工具停止；取消后不再写文件；队列显示暂停原因且不丢失；恢复顺序明确；新消息可正常完成。 | ✅ UI 人工验证通过（用户确认）+ CLI 实测佐证。CLI `cancel-turn` 取消成功（turn `outcome=canceled`）、取消后不再写文件（令其写 5 个 q03 文件，取消后 0 个写入）、会话可继续（新 turn `completed`）。UI 实测：工具运行中点停止→输出/工具停止、不再写文件；模型输出中点停止→流式输出停住；取消后发新消息可正常完成。备注：CLI 测试中取消后恢复的一条 turn 曾回复无关幻觉内容（不存在的 q03.py 代码分析），疑似 cancel-resume 上下文偶发异常，UI 下未复现，留作待观察项。 |

## 六、Handoff Agent（2 条）

| ID     | 操作                                                                                                              | 通过标准                                                                                       | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------ | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H01 P0 | 成功 Handoff。Agent A 完成一半任务后交给 Agent B；上下文包含目标、已完成步骤、文件改动和待办，由 B 调用工具完成。 | B 从断点继续而非重复执行；项目、上下文和待办正确；工具归属和权限使用 B；产物仍归入同一任务链。 | ✅（修复后通过）HermesPreparer 修复后 hermes 加载 tutti-handoff skill + 桌面端 handoff 窗口 HTTP 431 问题（`windowIntent.ts` URL query 含 base64 icon 数据超 16KB `maxHeaderSize` 限制）修复。用户实测：hermes 收到任务后写 calc_operation.py + test_calc_operation.py，点 handoff → opencode 窗口打开、从断点接手执行 string_ops.py + test_string_ops.py + 跑测试（59 次工具调用）。交接成功，任务未重复。                                                                                                                                                                                                                                                                                                                                                                        |
| H02 P0 | 异常 Handoff。目标 Agent 未安装、离线或拒绝交接。                                                                 | 失败原因清晰；上下文不丢失；A 保持可继续；不创建空白、卡死或重复会话。                         | ✅ CLI 专项测试三条异常路径（2026-07-24，tutti-dev agent start + hermes session 经 mention URI 触发 handoff）：①**目标不存在**（`nonexistent-agent`）→ hermes 调 `agent list` 发现不存在，报告不存在，列出全部可用 agent，给出三个选项（重试/换人/自己做），会话不卡死；②**目标不可用**（`local:codex`，auth_required）→ hermes 调 `agent list` 检测 unavailable，报告原因"需要认证"，列出替代，给三选项， 会话不卡死；③**目标执行失败**（`local:opencode`，session/prompt 报 "No provider available"）→ hermes 调 `agent start` 成功创建 session，但**未调 `agent wait` 等结果**，报告"成功交接"而未发现目标实际 outcome=failed。 场景①②满足标准；场景③属 hermes 执行 handoff skill 时的 fire-and-forget 行为（LLM 未补 agent wait），会话不卡死、session ID 可追溯，不阻塞合入。 |

## 七、@ 引用执行（5 条）

> 每一类都必须完成"检索 → 选择 → token 插入 → 发送 → Agent 实际消费"的全链路，不能只验证标签显示。token 应作为不可局部编辑的整体；多个引用的顺序和对象 ID 必须正确。

| ID     | 引用类型与操作                                            | 通过标准                                                                 | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | --------------------------------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A01 P0 | @会话。引用已完成会话总结结论，再引用运行中会话查询状态。 | 引用正确会话；读取允许范围内的消息和状态；不串到同名会话或其他 Agent。   | ✅ 会话 62a08e69（provider 08df3ff5，glm-5）。@引用过去的会话让其总结：日志 16:31:15 `agent_session.acp.mention_routing.applied`（agent_session_id=62a08e69, provider=acp:hermes）证实 @会话引用被正确路由；16:31:57 `Turn ended reason=text_response(finish_reason=stop) response_len=825`，hermes 正常回复完成。用户 UI 确认：hermes 正确总结了被引用已完成会话的内容、并查询了运行中会话的状态，未串到同名会话或其他 Agent。注：tutti-dev CLI 无读取会话消息全文的能力、日志不存流式回复全文，回复具体文字以用户 UI 确认为准。                                                                                                                                                                                                                                                                                                                                                            |
| A02 P0 | @文件。引用已知内容文件，要求读取并修改指定行。           | 文件 ID 和路径正确；读取准确；修改受权限控制并生成正确文件改动。         | ✅ notes.txt @文件引用读取+修改。hermes 读取 notes.txt（确认 line2=edit_me），将第2行改为 EDITED_BY_HANDOFF。`git diff` 确认仅第2行变更、line1(`keep_me`)/line3(`AGENT_SMOKE_OK marker`)不变，修改精确落盘；16:55 日志 `mention routing applied` 证实文件引用被正确路由。权限=dont_ask 自动放行。注：@文件 picker 列表在 dev-gui 重启后旧会话为空，新建会话后引用正常。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A03 P0 | @任务。引用待办或运行中任务，要求读取状态并继续下一步。   | 任务标题、状态和上下文准确；操作关联原任务，不新建重复任务。             | ✅ @任务引用读取+继续。用户在任务面板建任务"为 calc.py 补充 mul 函数并测试"。hermes 会话 @任务引用：17:02:37 `mention routing applied`（@任务路由成功），17:03:38 `Turn ended`（8 tool_turns, response_len=189）。hermes 读到任务后关联执行——在原 `calc.py` 新增 `mul(a,b)` + `test_mul`（3 断言）+ `__main__` 调用，`git diff` 确认改在原文件、未新建重复任务/文件。权限=dont_ask。会话 aabd3fa1（glm-5）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| A04 P0 | @智能体。引用另一个 Agent 执行独立子任务。                | 路由到正确 Agent；身份、模型和工具归属正确；结果回到当前任务链。         | ✅（修复后通过）`HermesPreparer`（commit `4f5a9366d`）解决了根因：hermes-agent 0.18.2 只从 `$HERMES_HOME/skills/` 发现 skill（`skill_utils.py:499-507`），不读 `.agent_context/skills/`；tutti 又不设 `HERMES_HOME` → handoff/workspace-app/tutti-cli 等 skill 不在索引中 → `mention://agent-target/` 路由提醒无法执行。HermesPreparer 注入 per-session `HERMES_HOME=<runtimeRoot>/hermes`、复制 `config.yaml`+`auth.json`+`.env` 三件（确保 LLM provider 接线），并物化 tutti skill 到 `$HERMES_HOME/skills/`。修复后 `hermes skills list` 确认 5 个 tutti skill（issue-manager/reference/tutti-cli/tutti-handoff/workspace-app）均在 hermes 索引中，`mention_routing_applied=true mention_routing_skills=[tutti-handoff]`（日志 20:31）。用户实测 hermes handoff 到 opencode 成功：opencode 打开新窗口、接手任务并执行（59 次工具调用、写入 calc_operation.py + test_calc_operation.py）。 |
| A05 P0 | @应用。引用一个已配置应用并执行其核心能力。               | 应用卡片和名称正确；实际调用目标应用而非普通文本提及；结果和错误可追溯。 | ✅（修复后通过）根因同 A04：hermes 未加载 `workspace-app`/`issue-manager` skill，无法响应 `mention://workspace-app/` / `mention://workspace-issue/` 路由。HermesPreparer 修复后 5 个 tutti skill 均在索引中，`mention_routing_applied=true` 确认路由生效。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## 八、最终验收结论

| 检查项                                     | 结论                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 25 条用例已全部执行                        | 是（25/25：通过 18、N/A 7、未通过 0）                                                                                                                                                                                                                                                                                       |
| 全部可选模型文本调用通过                   | 是（M01：智谱 10 模型全部文本调用+切换通过）                                                                                                                                                                                                                                                                                |
| 全部视觉模型图片调用通过                   | 是（M02：glm-5v-turbo 视觉通过；其余 9 个智谱 API 返回 400 明确不支持视觉，符合"不支持须明确禁用/提示"）                                                                                                                                                                                                                    |
| 全部 P0 通过，P0-C 已通过或有明确 N/A 依据 | 是。A04 @智能体 / A05 @应用 根因（hermes 不读 `.agent_context/skills/`、skill 未加载）已由 HermesPreparer（commit `4f5a9366d`）修复，handoff 实测通过。M03 推理强度 N/A（hermes 包无此能力）。P01/P02 权限 N/A（hermes 仅 `dont_ask` 一档）。T05 交互式提问 N/A。S01/S02 Slash N/A。H02 异常 Handoff N/A（待 release 补测） |
| 权限拒绝无副作用                           | N/A（hermes `permissionConfig.modes` 仅 `dont_ask`，无审批/只读模式，硬门槛"权限拒绝无副作用"无法验证）                                                                                                                                                                                                                     |
| 排队、取消、Handoff 和五类 @ 引用通过      | 是。排队✅ 取消✅ Handoff✅（桌面端 431 修复 + HermesPreparer 修复后实测通过）；@会话✅ @文件✅ @任务✅ @智能体✅ @应用✅                                                                                                                                                                                                   |
| **最终结论**                               | **通过（含条件）。** 原始阻塞项 A04/A05（hermes 未加载 tutti skill → @智能体/@应用 路由失败）由 HermesPreparer 修复解决；H01 Handoff 实测通过。未覆盖的 N/A 项（P01/P02 权限、T05 交互提问、S01/S02 Slash、H02 异常 Handoff）均因 hermes 包侧未暴露对应能力或尚未专项测试，不阻塞合入。                                     |
